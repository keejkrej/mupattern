[project]
name = "mupattern"
version = "0.1.0"
description = "Micropattern analysis tools"
requires-python = ">=3.12"
dependencies = ["mupattern-py"]

[dependency-groups]
dev = ["pytest>=8"]

[tool.uv.workspace]
members = ["mupattern-py"]

[tool.uv.sources]
mupattern-py = { workspace = true }
