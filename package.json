{
  "private": true,
  "workspaces": ["mupattern-desktop", "mupattern-web", "packages/shared"],
  "scripts": {
    "dev:desktop": "bun run --cwd mupattern-desktop dev",
    "dev:web": "bun run --cwd mupattern-web dev",
    "build:web": "bun run --cwd mupattern-web build",
    "build:desktop": "bun run --cwd mupattern-desktop build"
  }
}
